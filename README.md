# ISM 2411 Data Cleaning Project
This repository contains a Python script that cleans a messy sales dataset. The script standardizes column names, fills missing values, removes invalid rows, and saves a cleaned CSV for analysis.

